<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 配置全局属性 -->
    <settings>
        <!-- 使用jdbc的getGeneratedKeys获取数据库自增主键值 -->
        <setting name="useGeneratedKeys" value="true" />

        <!-- 使用列别名替换列名 默认:true -->
        <setting name="useColumnLabel" value="true" />

        <!-- 开启驼峰命名转换:Table{create_time} -> Entity{createTime} -->
        <setting name="mapUnderscoreToCamelCase" value="true" />

        <!--延迟加载的全局开关。当开启时，所有关联对象都会延迟加载。-->
        <setting name="lazyLoadingEnabled" value="false"/>
    </settings>

    <typeAliases>
        <typeAlias type="com.sherry.entity.Book" alias="Book"/>
        <typeAlias type="com.sherry.entity.Admin" alias="Admin"/>
        <typeAlias type="com.sherry.entity.Type" alias="Type"/>
        <typeAlias type="com.sherry.entity.Card" alias="Card"/>
        <typeAlias type="com.sherry.entity.Valid" alias="Valid"/>
        <typeAlias type="com.sherry.entity.Identity" alias="Identity"/>
    </typeAliases>

</configuration>