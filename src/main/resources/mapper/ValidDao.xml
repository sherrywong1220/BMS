<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sherry.dao.ValidDao">

    <select id="queryAllValids" resultType="Valid">
        SELECT * FROM t_valid
    </select>
</mapper>